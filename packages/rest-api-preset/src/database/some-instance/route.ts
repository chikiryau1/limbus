import { Router } from 'express';

// import { getInstance, getInstanceList } from './controller';

const router = Router();

// router.get('/:id', getInstance);

// router.get('/', getInstanceList);

export default router;
